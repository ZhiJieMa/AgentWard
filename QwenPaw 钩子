  一、Agent 生命周期钩子（核心层）
                                                                                                                                                                                                                                                                                                                               
  这是最重要的一层，由 AgentScope 框架驱动，在 react_agent.py:449 注册。

  ┌───────────────┬─────────────────────────────────┬──────────────────────────────────────┬────────────────────────────┐
  │   钩子类型    │            触发时机             │                 签名                 │            用途            │
  ├───────────────┼─────────────────────────────────┼──────────────────────────────────────┼────────────────────────────┤
  │ pre_reply     │ 每次 agent 向用户输出最终回复前 │ (agent, kwargs) → dict | None        │ 内存检索、消息增强         │
  ├───────────────┼─────────────────────────────────┼──────────────────────────────────────┼────────────────────────────┤
  │ pre_reasoning │ 每次推理步骤前（LLM 调用前）    │ (agent, kwargs) → dict | None        │ 上下文检查/压缩、首次引导  │
  ├───────────────┼─────────────────────────────────┼──────────────────────────────────────┼────────────────────────────┤
  │ post_acting   │ 每次工具调用完成后              │ (agent, kwargs, output) → Msg | None │ 工具结果修剪、截断超大输出 │
  ├───────────────┼─────────────────────────────────┼──────────────────────────────────────┼────────────────────────────┤
  │ post_reply    │ 每次最终回复完成后              │ (agent, kwargs, output) → Msg | None │ 自动内存保存、遥测         │
  └───────────────┴─────────────────────────────────┴──────────────────────────────────────┴────────────────────────────┘

  注册方式：
  self.register_instance_hook(
      hook_type="pre_reasoning",   # 四种类型之一
      hook_name="my_hook",
      hook=my_callable,
  )

  返回值语义：
  - pre_* 钩子：返回 None 不修改，返回 dict 替换 kwargs
  - post_* 钩子：返回 None 不修改，返回 Msg 替换输出消息

  ---
  二、插件系统钩子（应用层）

  由 PluginRegistry 管理，通过 PluginApi 注册，在 FastAPI lifespan 中执行。

  ┌───────────────┬─────────────────────────────────────────┬───────────────────────┬──────────────────────┐
  │   钩子类型    │                触发时机                 │         签名          │         用途         │
  ├───────────────┼─────────────────────────────────────────┼───────────────────────┼──────────────────────┤
  │ startup_hook  │ 应用启动时（FastAPI lifespan startup）  │ () → None | Awaitable │ 注册工具、初始化 SDK │
  ├───────────────┼─────────────────────────────────────────┼───────────────────────┼──────────────────────┤
  │ shutdown_hook │ 应用关闭时（FastAPI lifespan shutdown） │ () → None | Awaitable │ 清理资源、关闭连接   │
  └───────────────┴─────────────────────────────────────────┴───────────────────────┴──────────────────────┘

  注册方式（插件内）：
  api.register_startup_hook(
      hook_name="init_my_tool",
      callback=self.on_startup,
      priority=50,   # 数值越小越先执行
  )

  执行顺序： 按 priority 升序（低值优先），支持 sync/async 混用。

  ---
  三、控制命令钩子（命令层）

  PluginRegistry 还支持注册自定义控制命令处理器，扩展内置命令集。

  内置命令集（command_handler.py:38）：
  /compact  /new  /clear  /history  /compact_str
  /summarize_status  /message  /dump_history  /load_history
  /proactive  /plan

  注册自定义命令：
  api.register_control_command(
      handler=MyCommandHandler(),   # 实现 BaseControlCommandHandler
      priority_level=10,
  )

  ---
  四、你的需求对应的钩子选择

  ┌──────────────────────────────┬───────────────────────────┬─────────────────────────────────────────────┐
  │             需求             │         推荐钩子          │                    理由                     │
  ├──────────────────────────────┼───────────────────────────┼─────────────────────────────────────────────┤
  │ 配置检测                     │ startup_hook (priority=0) │ 应用启动时最早执行，可阻断启动              │
  ├──────────────────────────────┼───────────────────────────┼─────────────────────────────────────────────┤
  │ skill 安全检测               │ pre_reasoning             │ LLM 调用前拦截，可检查 skill 调用意图       │
  ├──────────────────────────────┼───────────────────────────┼─────────────────────────────────────────────┤
  │ 助理消息 toolUse 检测        │ post_acting               │ 工具调用完成后，可拿到完整 tool_use 内容    │
  ├──────────────────────────────┼───────────────────────────┼─────────────────────────────────────────────┤
  │ 工具调用结果 toolResult 检测 │ post_acting               │ output 参数即为工具结果 Msg                 │
  ├──────────────────────────────┼───────────────────────────┼─────────────────────────────────────────────┤
  │ 命令检测                     │ register_control_command  │ 注册自定义命令处理器，或在 pre_reply 中拦截 │
  └──────────────────────────────┴───────────────────────────┴─────────────────────────────────────────────┘

  ---
  五、现有钩子实现参考

  - BootstrapHook — src/qwenpaw/agents/hooks/bootstrap.py：最简单的 pre_reasoning 钩子，可作为模板
  - LightContextManager — src/qwenpaw/agents/context/light_context_manager.py：完整实现了全部 4 个生命周期钩子
  - BaseContextManager — src/qwenpaw/agents/context/base_context_manager.py：抽象接口，定义了标准签名

  开发插件时，pre_reasoning 和 post_acting 是最关键的两个扩展点，覆盖了你列出的大部分检测场景。












 直接结论：不能直接平移，必须修改                                                                                                                                                                                                                                                                                                                                ns)
  两边的差距不是语言问题，是钩子体系的深度完全不同。                                                                                                                                                                                                                                                                         
  核心差距

  ┌──────────────┬────────────────────────────────────────────────────────────────┬───────────────────────────────┐
  │     维度     │                         OpenClaw (TS)                          │       QwenPaw (Python)        │
  ├──────────────┼────────────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 钩子总数     │ 33 个                                                          │ 6 个（含 Agent 生命周期）     │
  ├──────────────┼────────────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 消息级钩子   │ before_agent_reply, llm_input, llm_output, message_received 等 │ 无                            │
  ├──────────────┼────────────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 工具调用钩子 │ before_tool_call, after_tool_call                              │ 无（只有 post_acting 粗粒度） │
  ├──────────────┼────────────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 钩子数据结构 │ 完整事件对象 + 结构化返回值（handled, block 等）               │ 简单 kwargs + 可选 Msg 替换   │
  ├──────────────┼────────────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 命令检测     │ 专用钩子                                                       │ register_control_command      │
  └──────────────┴────────────────────────────────────────────────────────────────┴───────────────────────────────┘

  你的四个业务场景在 QwenPaw 里的对应情况：

  配置检测        → startup_hook          ✅ 可直接对应
  skill 安全检测  → pre_reasoning        ⚠️粒度不同，需改写逻辑
  工具结果检测    → post_acting          ⚠️只有结果，没有 before_tool_call
  命令检测        → register_control_command⚠️机制不同

  ---
  你有两条路

  路线 A：改写业务逻辑适配 QwenPaw 现有钩子

  把 TS 插件的业务逻辑翻译成 Python，映射到现有的 6 个钩子点。

  - 优点：不需要改 QwenPaw 核心代码，稳定
  - 缺点：before_agent_reply 那套拦截+交互逻辑在 pre_reasoning 里实现会很别扭，你上个 session 遇到的"消息丢失"问题在这里同样存在

  路线 B：给 QwenPaw 扩展缺失的钩子，再平移业务

  在 QwenPaw 的 PluginRegistry 和 react_agent.py 里补充你需要的钩子类型，然后业务逻辑几乎原样翻译。

  - 优点：业务逻辑改动最小，钩子语义和 OpenClaw 对齐
  - 缺点：需要改 QwenPaw 核心，要维护 fork

  ---
  我的建议

  选路线 B，但只补你实际用到的钩子，不需要全部 33 个。根据你的四个业务场景，只需要补这几个：

  ┌────────────────────┬────────────────────┬────────────────────────────────┐
  │    需要补的钩子    │   对应 OpenClaw    │            插入位置            │
  ├────────────────────┼────────────────────┼────────────────────────────────┤
  │ before_tool_call   │ before_tool_call   │ react_agent.py _acting() 前    │
  ├────────────────────┼────────────────────┼────────────────────────────────┤
  │ after_tool_call    │ after_tool_call    │ post_acting 已有，可复用       │
  ├────────────────────┼────────────────────┼────────────────────────────────┤
  │ before_agent_reply │ before_agent_reply │ pre_reply 已有，语义接近       │
  ├────────────────────┼────────────────────┼────────────────────────────────┤
  │ llm_input          │ llm_input          │ _reasoning() 前，拿到 messages │
  └────────────────────┴────────────────────┴────────────────────────────────┘

  实际上 pre_reply ≈ before_agent_reply，post_acting ≈ after_tool_call，真正缺的只有 before_tool_call 和 llm_input。

  你想走哪条路？如果选路线 B，我可以帮你在 QwenPaw 里补这两个钩子点。
